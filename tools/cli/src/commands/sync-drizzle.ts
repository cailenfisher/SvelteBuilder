export async function syncDrizzle(_root?: string): Promise<void> {
  console.log('[sveltebuilder] sync:drizzle — not yet implemented');
  console.log('[sveltebuilder] Native (Drizzle-managed datasource) support is coming in a future release.');
  console.log('[sveltebuilder] For SuperPrototype (Supabase), run: sveltebuilder sync:supabase');
}
